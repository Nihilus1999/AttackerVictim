import com.ucab.cmcapp.implementation.AutenticacionService;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;
import com.ucab.cmcapp.logic.dtos.extras.CredencialesDto;
import javax.ws.rs.core.Response;

public class AutenticacionServiceTest {
/*
    @Test
    public void testAuthenticateAdministrador() {
        // Arrange
        CredencialesDto credenciales = new CredencialesDto();
        credenciales.set_alias("admin");
        credenciales.set_clave("password");

        // Act and Assert
        Assertions.assertThrows(RuntimeException.class, () -> {
            AutenticacionService autenticacionService = new AutenticacionService();
            Response response = autenticacionService.authenticateAdmin(credenciales);

            // Verificar el estado de la respuesta según el resultado esperado
            Assertions.assertEquals(Response.Status.OK.getStatusCode(), response.getStatus());

            // Verificar más detalles según el resultado esperado
            // ...
        });
    }

    @Test
    public void testAuthenticateUsuario() {
        // Arrange
        CredencialesDto credenciales = new CredencialesDto();
        credenciales.set_alias("admin");
        credenciales.set_clave("password");

        // Act and Assert
        Assertions.assertThrows(RuntimeException.class, () -> {
            AutenticacionService autenticacionService = new AutenticacionService();
            Response response = autenticacionService.authenticateUsuario(credenciales);

            // Verificar el estado de la respuesta según el resultado esperado
            Assertions.assertEquals(Response.Status.OK.getStatusCode(), response.getStatus());

            // Verificar más detalles según el resultado esperado
            // ...
        });
    }
*/

}

